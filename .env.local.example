# Copy this file to .env.local and replace the placeholder with your real key.
# Keep this file out of source control (don't commit your real .env.local).

# Google Gemini API key used by server-side `/api/advice`.
GEMINI_API_KEY=your_gemini_api_key_here
